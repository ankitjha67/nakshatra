# --- core (required) ---
fastapi>=0.136.3
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-dateutil>=2.9.0.post0
httpx>=0.28.1

# Swiss Ephemeris: pysweph is the maintained fork with wheels for current
# Python (original pyswisseph has none past 3.11). Provides `import swisseph`.
pysweph>=2.10.3.6

# --- LLM provider: Vertex / Gemini ---
google-genai>=2.8.0

# --- auth + store (Firebase / Firestore) ---
firebase-admin>=7.4.0
google-cloud-firestore>=2.27.0

# --- security floors for transitive deps (pin >= the patched version; pip-audit/CI flags these) ---
starlette>=1.3.1          # CVE-2026-54282 / CVE-2026-54283 (pulled in by fastapi)
cryptography>=48.0.1      # GHSA-537c-gmf6-5ccf (pulled in by firebase-admin / google libs)
msgpack>=1.2.1            # GHSA-6v7p-g79w-8964 (transitive)
